# Prefer repo-root `.env`. Never commit real DB passwords.
LLM_PROVIDER=auto
GROQ_API_KEY=
GROQ_MODEL=llama-3.1-8b-instant
OLLAMA_MODEL=
GRADIO_HOST=127.0.0.1
GRADIO_PORT=7866
# Optional remote DB (MySQL example — use your own secrets):
# DATABASE_URL=mysql+mysqlconnector://user:pass@host:3306/Chinook
